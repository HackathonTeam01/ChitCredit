# Final C-Part Handoff

## Automated checks completed

- Python C-engine tests: `6 passed`.
- Express C API tests: `4 passed`.
- Backend TypeScript check: passed.
- Frontend production build: passed.

## Human-only actions before submission

- [ ] Set real `SUPABASE_URL` and `SUPABASE_KEY` values.
- [ ] Start backend and frontend from a clean terminal.
- [ ] Run the member flow: score -> smoothing -> repayment slider -> contribution -> notification.
- [ ] Run the operator flow and capture three screenshots for the backup slide.
- [ ] Time the four-minute pitch once solo and once with the team.
- [ ] Confirm team names, roles, deck format, and portal deadline.
- [ ] Submit the deck and repository through the official portal.

The repository cannot truthfully mark the human-only items complete without the team's credentials, browser session, and portal access.
